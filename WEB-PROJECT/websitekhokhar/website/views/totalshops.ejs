<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Total Registered Shops</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
 <!-- Replace this CSS section in your <style> block -->
<style>
  body {
    margin: 0;
    font-family: 'Poppins', sans-serif;
    background-color: #000;
    color: #fff;
  }

  /* Navbar */
  .navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: #111;
    padding: 15px 50px;
    box-shadow: 0 2px 10px red;
  }

  .navbar .logo {
    font-size: 1.8em;
    font-weight: bold;
    color: #ff1e1e;
    letter-spacing: 1px;
  }

  .navbar ul {
    list-style: none;
    display: flex;
    gap: 25px;
  }

  .navbar ul li a {
    color: white;
    text-decoration: none;
    font-size: 1em;
    transition: color 0.3s;
  }

  .navbar ul li a:hover {
    color: #ff1e1e;
  }

  /* Hero Section */
  .hero {
    background: url('https://images.unsplash.com/photo-1582407947304-7f7cf7a3d2e0?auto=format&fit=crop&w=1500&q=80') no-repeat center center/cover;
    height: 300px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
    position: relative;
  }

  .hero::after {
    content: '';
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    background: rgba(0, 0, 0, 0.7);
  }

  .hero h1 {
    font-size: 3em;
    z-index: 1;
    position: relative;
    color: #ff1e1e;
    text-shadow: 0 0 10px #ff1e1e;
  }

  /* Shops Grid */
  .shop-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 30px;
    padding: 40px 60px;
  }

  .shop-card {
    background: #1a1a1a;
    border-radius: 15px;
    padding: 25px;
    text-align: center;
    transition: transform 0.3s, box-shadow 0.3s;
    border: 1px solid #2a2a2a;
    box-shadow: 0 4px 20px rgba(255, 30, 30, 0.2);
  }

  .shop-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 0 25px #ff1e1e, 0 0 10px #ff1e1e inset;
  }

  .shop-logo {
    width: 100px;
    height: 100px;
    object-fit: cover;
    border-radius: 50%;
    border: 3px solid #ff1e1e;
    margin-bottom: 15px;
  }

  .shop-name {
    font-size: 1.3em;
    font-weight: 600;
    color: #fff;
    margin-bottom: 5px;
  }

  .shop-address {
    font-size: 0.95em;
    color: #bbb;
  }

  a {
    text-decoration: none;
    color: inherit;
  }

  .no-shops {
    text-align: center;
    padding: 50px;
    font-size: 1.2em;
    color: #aaa;
  }

  /* Footer */
  .footer {
    background: #111;
    padding: 30px 50px;
    text-align: center;
    border-top: 1px solid #2a2a2a;
    color: #aaa;
    font-size: 0.9em;
  }

  @media (max-width: 600px) {
    .navbar {
      flex-direction: column;
      padding: 15px;
    }

    .hero h1 {
      font-size: 2em;
    }

    .shop-container {
      padding: 20px;
    }
  }
</style>

</head>
<body>

  <!-- Navbar -->
  <nav class="navbar">
    <div class="logo">Buy & Shop</div>
    <ul>
      <li><a href="/">Home</a></li>
      <li><a href="#shops">Shops</a></li>
      <li><a href="#about">About Us</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
  </nav>

  <!-- Hero Section -->
  <section class="hero">
    <h1>Total Registered Shops</h1>
  </section>

  <!-- Shop Cards -->
  <% if (shops.length === 0) { %>
    <p class="no-shops">No approved shops found.</p>
  <% } else { %>
    <div class="shop-container" id="shops">
      <% shops.forEach(shop => { %>
        <a href="/shoppage/<%= shop._id %>" target="_blank" class="shop-card">
          <img src="<%= shop.logo %>" alt="Logo" class="shop-logo">
          <div class="shop-name"><%= shop.name %></div>
          <div class="shop-address"><%= shop.address %></div>
        </a>
      <% }) %>
    </div>
  <% } %>

  <!-- Footer -->
  <footer class="footer" id="contact">
    &copy; <%= new Date().getFullYear() %> Buy & Shop. All rights reserved. |
    Contact: <a href="mailto:info@buyandshop.com" style="color:#ff1e1e;">info@buyandshop.com</a>
  </footer>

</body>
</html>
