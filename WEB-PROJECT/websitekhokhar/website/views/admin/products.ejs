<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Buy & Shop - Admin Panel</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
  <style>
    :root {
      --bg-overlay: rgba(0, 0, 0, 0.6);
      --sidebar-bg: #292929;
      --card-bg: #333;
      --topbar-bg: #3b3b3b;
      --text-light: #f1f1f1;
      --text-muted: #ccc;
      --btn-bg-dark: #4f6f96;
      --btn-bg-dark-hover: #3d4f77;
      --btn-bg-light: #6c7a98;
      --btn-bg-light-hover: #5a678b;
      --product-bg-dark: #2c3e50;
      --product-bg-light: #f5f5f5;
    }

    body {
      background: url('https://www.loucampaio.com/wp-content/plugins/admin-custom-login/images/3d-background.jpg') no-repeat center center fixed;
      background-size: cover;
      color: var(--text-light);
      font-family: 'Segoe UI', sans-serif;
      position: relative;
      z-index: 0;
      margin: 0;
      padding: 0;
      height: 100vh;
    }

    body::before {
      content: '';
      position: absolute;
      top: 0; left: 0;
      width: 100%;
      height: 100%;
      background-color: var(--bg-overlay);
      z-index: -1;
    }

    .light-mode {
      --bg-overlay: rgba(255, 255, 255, 0.7);
      --sidebar-bg: #e0e0e0;
      --card-bg: #f9f9f9;
      --topbar-bg: #f1f1f1;
      --text-light: #333;
      --text-muted: #555;
      --btn-bg-dark: #0066cc;
      --btn-bg-dark-hover: #004b99;
      --btn-bg-light: #00aaff;
      --btn-bg-light-hover: #0088cc;
      --product-bg-dark: #ececec;
      --product-bg-light: #f5f5f5;
    }

    .overlay {
      display: flex;
      height: 100%;
      flex: 1;
    }

    .sidebar {
      width: 260px;
      background: var(--sidebar-bg);
      padding: 30px 15px;
      border-right: 1px solid #333;
      height: 100vh;
      display: flex;
      flex-direction: column;
      position: fixed;
    }

    .sidebar h2 {
      color: var(--text-light);
      font-size: 24px;
      margin-bottom: 40px;
      text-align: center;
    }

    .sidebar a {
      display: block;
      padding: 14px 20px;
      margin-bottom: 10px;
      color: var(--text-muted);
      border-radius: 8px;
      text-decoration: none;
      transition: all 0.2s ease;
    }

    .sidebar a:hover,
    .sidebar a.active {
      background: var(--btn-bg-dark);
      color: #fff;
    }

    .main-content {
      flex: 1;
      padding: 30px;
      margin-left: 260px;
      overflow-y: auto;
    }

    .topbar {
      background: var(--topbar-bg);
      border-radius: 12px;
      padding: 20px 30px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 30px;
      box-shadow: 0 0 10px rgba(0,0,0,0.5);
    }

    .topbar .user {
      display: flex;
      align-items: center;
      gap: 15px;
      color: var(--text-muted);
    }

    .topbar .user img {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      object-fit: cover;
    }

    .dashboard-cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 24px;
      margin-bottom: 40px;
    }

    .card-box {
      background: var(--card-bg);
      padding: 24px;
      border-radius: 14px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.4);
      transition: transform 0.2s ease;
    }

    .card-box:hover {
      transform: translateY(-4px);
      background: var(--btn-bg-dark-hover);
    }

    .card-box h5 {
      margin-bottom: 10px;
      color: var(--text-muted);
      font-weight: 500;
    }

    .card-box .value {
      font-size: 24px;
      font-weight: bold;
      color: var(--text-light);
    }

    .product-section {
      background: var(--product-bg-dark);
      padding: 24px;
      border-radius: 14px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.5);
    }

    .product-section h4 {
      margin-bottom: 20px;
      color: var(--text-light);
    }

    .btn-custom {
      background: var(--btn-bg-dark);
      color: #fff;
      border: none;
      border-radius: 8px;
      padding: 10px 20px;
      transition: background-color 0.3s;
    }

    .btn-custom:hover {
      background: var(--btn-bg-dark-hover);
    }

    .table-responsive {
      margin-top: 20px;
      border: 1px solid #333;
      border-radius: 10px;
      overflow: hidden;
    }

    .table-dark {
      background: var(--product-bg-light);
      color: var(--text-light);
    }

    .table-dark th, .table-dark td {
      vertical-align: middle;
      padding: 12px;
      text-align: center;
    }

    .table-dark th {
      background-color: var(--btn-bg-dark);
      color: #fff;
    }

    .table-dark tbody tr:hover {
      background-color: var(--btn-bg-dark-hover);
    }

    .footer {
      text-align: center;
      margin-top: 60px;
      color: #aaa;
      font-size: 14px;
    }

    .toggle-switch {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .form-check-input:checked {
      background-color: var(--btn-bg-dark);
      border-color: var(--btn-bg-dark);
    }

    @media (max-width: 768px) {
      .overlay {
        flex-direction: column;
      }

      .sidebar {
        width: 100%;
        height: auto;
        position: relative;
        border-right: none;
        border-bottom: 1px solid #444;
      }

      .main-content {
        padding: 20px;
        margin-left: 0;
      }
    }
  </style>
</head>
<body>
  <div class="overlay">
    <!-- Sidebar -->
    <div class="sidebar">
      <h2>Admin Panel</h2>
      <a href="/shop/shoppage" class="active"><i class="fas fa-chart-line me-2"></i> HomePage</a>
      <a href="/productspage"><i class="fas fa-boxes me-2"></i> Products</a>
      
      <a href="/shop/orders"><i class="fas fa-shopping-cart me-2"></i>View Order</a>

      <a href="/shoppage" ><i class="fas fa-users me-2"></i> Customers</a>
      <a href="/admin/settings"><i class="fas fa-cogs me-2"></i> Settings</a>
    </div>

    <!-- Main Content -->
    <div class="main-content">
      <!-- Topbar -->
      <div class="topbar">
        <h4 class="m-0">Welcome Back</h4>
        <div class="toggle-switch">
          <label class="form-check-label text-light" for="modeToggle">Light Mode</label>
          <input class="form-check-input" type="checkbox" id="modeToggle" />
        </div>
        <div class="user">
          <img src="https://i.pravatar.cc/100" alt="Admin Avatar" />
          <span>Admin</span>
        </div>
      </div>

      <!-- Dashboard Cards -->
      <div class="dashboard-cards">
        <div class="card-box">
  <h5>Total Sales</h5>
  <div class="value">PKR <%= totalSales.toLocaleString() %></div>
</div>
        <div class="card-box"><h5>Total Orders</h5><div class="value"><%= totalOrders %></div></div>
        <div class="card-box">
  <h5>New Orders</h5>
  <div class="value" id="new-orders-count"><%= newOrdersCount %></div>
</div>
        <div class="card-box"><h5>Pending Orders Shipments</h5><div class="value"><%= pendingShipmentsCount %></div></div>
      </div>

 <!-- Product Management Section -->
<div class="product-section mt-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h4>Product Management</h4>
    <a href="/admin/products/create" class="btn btn-custom"><i class="fas fa-plus me-2"></i>Add New Product</a>
  </div>

  <table class="table table-dark table-responsive">
    <thead>
      <tr>
        <th>ID</th>
        <th>Title</th>
        <th>Price</th>
        <th>Stock</th>
        <th>Description</th>
        <th>Picture</th>
        <th>Category</th> <!-- ✅ Added Category column -->
        <th>Actions</th>
      </tr>
    </thead>
    <tbody id="productTable">
      <% for (let index = 0; index < products.length; index++) { %>
        <tr>
          <td><%= products[index]._id %></td>
          <td class="product-title"><%= products[index].title %></td>
          <td><%= products[index].price %></td>
          <td><%= products[index].stock %></td>
          <td><%= products[index].description %></td>
          <td><img src="/<%= products[index].picture %>" width="100px" /></td>
          <td>
            <%= products[index].category ? products[index].category.name : "No Category" %> <!-- ✅ Show category name -->
          </td>
          <td>
            <a href="/admin/products/edit/<%= products[index]._id %>" class="btn btn-warning">Edit</a>
            <a href="/admin/products/delete/<%= products[index]._id %>" class="btn btn-danger">Delete</a>
          </td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>

      <!-- Footer -->
      <div class="footer mt-5">
        &copy; 2025 Buy and Shop — All rights reserved
      </div>
    </div>
  </div>

  <script>
    document.getElementById("modeToggle").addEventListener("change", function () {
      document.body.classList.toggle("light-mode");
    });
  </script>
</body>
</html>
