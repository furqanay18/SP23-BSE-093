import 'package:flutter/material.dart';


class StockInPage extends StatelessWidget {
  const StockInPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Stock In')));
}
class Additems extends StatelessWidget {
  const Additems ({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Stock In')));
}


class StockOutPage extends StatelessWidget {
  const StockOutPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Stock Out')));
}

class MoveStockPage extends StatelessWidget {
  const MoveStockPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Move Stock')));
}

class AdjustPage extends StatelessWidget {
  const AdjustPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Adjust Stock')));
}

